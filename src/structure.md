# Structure
main.rs ->
iced App struct ->
- maintain music queue state
lib.rs ->
- get file info to load albums, artists, and genres (load_music_info(dir))
- play an audio file (play_file(file))
- modify file, e.g. ratings
 
